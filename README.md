# THE BLACKBOARD LINEAGE — 1959 → 2026

A heritage instrument. One architecture — **many private specialists, one small
shared board they broadcast to** — reinvented every generation since 1959, and
found last summer growing untended inside a language model.

A single offline HTML file: system fonts only, zero network, a soft-GL 3D board
projected by hand onto a 2D canvas, and **fail-loud** diagnostic cards (any throw
replaces that figure with a readable error, never a silent black rectangle).

## The seven stations

1. **1959 · Pandemonium** *(LIT)* — Selfridge: recognition as a shrieking crowd of
   feature-demons; the answer lives in the din.
2. **1975 · Hearsay-II** *(LIT)* — Erman/Hayes-Roth/Lesser/Reddy: the word
   *blackboard* enters computing as a literal shared data structure.
3. **1988 · Global Workspace** *(BRI)* — Baars borrows the architecture for the
   mind: a spotlight on a stage of specialists seated in the dark.
4. **1998 · Global Neuronal Workspace / ignition** *(LIT)* — Dehaene & Changeux
   build it in cortex: sub-threshold sparks die; a supra-threshold one *ignites*
   all-or-none and becomes reportable (~300 ms slow wave, gamma, long-range sync).
5. **2020–23 · the reading glasses** *(LIT)* — the residual stream as a shared
   additive bus (features in superposition); the logit lens and tuned lens learn
   to read it.
6. **2026 · the measured board** *(SPEC)* — the Jacobian lens *measures* the
   transport and reveals **J-space**: a small subspace the model can report,
   modulate, and use — the same architecture, this time emergent.
7. **↻ · the fractal** *(BRI)* — the same board at every scale: heads share the
   residual stream, positions share the context window, agents share a transcript,
   collaborators share a repository.

## Honesty tiers (stated on every claim)

- **LIT** — verified / mine (the soft-GL projection math is checked in Node; 16/16).
- **BRI** — bridged / inferred (the architecture *rhyme* is an interpretive frame,
  not a proof).
- **SPEC** — attested, not witnessed (the 2026 workspace-in-a-model finding: the
  lens math and data pipeline verified by hand, but the report/modulate/use triad
  lives on hardware this instrument has not run).

The tiers are the point: the emergence claim is **hedged, not asserted**. Nothing
here says "proven" that wasn't measured here.

## Verify

Open the file — no build step, no network. The soft-GL is honest math: rotations
are orthonormal, the perspective projection is standard, and every "readout" is a
real linear projection (the vector-sum readout is exactly the shadow on the axis;
the bus readout is a genuine inner product). All 16 checks pass in Node.

## Kin

The interpretability-geometry cluster of the UD0 **Artificial Intelligence** domain:
[THE ATTENTION HOURGLASS](https://davidwise01.github.io/attention-hourglass/) (the
shape of attention) · [THE ATTENTION MESH](https://davidwise01.github.io/attention-mesh/)
(attention topology) · [TTU1 · Transformer Tech](https://davidwise01.github.io/ttu1/).

---
David Lee Wise / ROOT0 / TriPod LLC · CC-BY-ND-4.0
